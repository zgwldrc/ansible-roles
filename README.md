# Setup

```
pip install ansible boto3

git clone https://github.com/zgwldrc/ansible-roles.git
cd ansible-roles
cp ansible.cfg{.tpl,}
cp aws_ec2.yml{.tpl,}
# change aws_ec2.yml and ansible.cfg as needed

# write your playbooks under playbooks dir
mkdir playbooks
```

