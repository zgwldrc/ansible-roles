[Unit]
Description="{{systemd_name}} {{version}}"
Requires=network-online.target
After=network-online.target

[Service]
EnvironmentFile=-{{home}}/.env
User={{user}}
Group={{group}}
WorkingDirectory={{home}}
ExecStart=/bin/bash {{home}}/{{systemd_name}}.sh
Restart=on-failure
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
