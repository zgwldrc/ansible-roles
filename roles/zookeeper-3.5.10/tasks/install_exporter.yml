- name: user create
  user:
    name: "{{user}}"
    state: present

- name: group create
  group:
    name: "{{group}}"
    state: present

- name: ensure home and relavtive dirs created
  file:
    path: "{{item}}"
    state: directory
    owner: "{{user}}"
    group: "{{group}}"
  loop:
    - "{{exporter_home}}"

- name: check if downloaded
  stat:
    path: "{{exporter_download_dest}}"
  register: exporter_downloaded

- name: "Download"
  when: not exporter_downloaded.stat.exists
  block:
  - name: download from ansible control node
    when: 
    - install_from_files
    copy:
      src: "{{exporter_archive_file}}"
      dest: "{{exporter_download_dest}}"

  - name: download from s3_url
    when: 
    - exporter_s3_url
    - not install_from_files
    shell: |
      aws s3 cp {{exporter_s3_url}} {{exporter_download_dest}}
  - name: download from urlpath
    when:
    - not exporter_s3_url
    - not install_from_files
    get_url:
      url: "{{exporter_urlpath}}"
      dest: "{{exporter_download_dest}}"

  
- name: check if installed
  stat:
    path: "{{exporter_home}}/zookeeper-exporter"
  register: execfile

- name: install
  when: not execfile.stat.exists
  block:
  - name: create temp dir
    tempfile:
      state: directory
    register: tempdir
  - name: unarchive install package
    unarchive:
      src: "{{exporter_download_dest}}"
      dest: "{{tempdir.path}}"
      remote_src: yes
  - name: sync home dir
    synchronize:
      src: "{{tempdir.path}}/zookeeper-exporter-v{{exporter_version}}-linux/"
      dest: "{{exporter_home}}/"
    delegate_to: "{{ inventory_hostname }}"
      
- name: exporter systemd unit
  template:
    src: exporter.unit
    dest: "/usr/lib/systemd/system/{{exporter_systemd_name}}.service"

- name: add service control shortcuts commands
  blockinfile:
    path: "/home/{{user}}/.bashrc"
    marker: "# {{item}} {mark} ANSIBLE MANAGED BLOCK"
    block: |
      alias zkeptstatus='sudo systemctl status {{item}}.service'
      alias zkeptstart='sudo systemctl start {{item}}.service'
      alias zkeptstop='sudo systemctl stop {{item}}.service'
      alias zkeptrestart='sudo systemctl restart {{item}}.service'
      alias zkeptlog='journalctl -u {{item}}.service -f -n 200 -o cat'
    create: true
    owner: "{{user}}"
    group: "{{group}}"
  loop:
    - "{{exporter_systemd_name}}"

- name: add suduers file
  blockinfile:
    path: "/etc/sudoers.d/{{user}}"
    create: true
    marker: "# {{item}} {mark} ANSIBLE MANAGED BLOCK"
    block: |
      {{user}} ALL=(root) NOPASSWD: /usr/bin/systemctl start {{item}}.service
      {{user}} ALL=(root) NOPASSWD: /usr/bin/systemctl stop {{item}}.service
      {{user}} ALL=(root) NOPASSWD: /usr/bin/systemctl restart {{item}}.service
      {{user}} ALL=(root) NOPASSWD: /usr/bin/systemctl status {{item}}.service
  loop:
    - "{{exporter_systemd_name}}"   

- name: ensure home dir owner and group
  file:
    path: "{{exporter_home}}"
    state: directory
    owner: "{{user}}"
    group: "{{group}}"
    recurse: yes

- name: "Enable exporter service"
  systemd:
    name: "{{exporter_systemd_name}}"
    enabled: yes
    state: started
    daemon_reload: true 