{%- for host in (ansible_play_hosts | sort()) -%}
{%- if 'controller' in hostvars[host]['rocketmq_role'] -%}
{{"{}:9878".format(hostvars[host]['ansible_default_ipv4']['address'])}}
{%- if not loop.last %};{% endif %}
{%- endif -%}
{%- endfor +%}